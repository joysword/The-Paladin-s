using UnityEngine;
using System.Collections;

public class LookUDScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        //if (getReal3D.Input.GetButton("WandLook"))
        //{
        //    float analogUD = -getReal3D.Input.GetAxis("Forward") * axisSensitivity;
         //   transform.Rotate(analogUD * Vector3.right);
       //}
	}
}
